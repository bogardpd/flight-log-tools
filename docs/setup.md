# Setup

If you want to install this module locally and access it from any folder, while still allowing the scripts to be edited after install, it may help to do a pip editable installation:

```bash
cd path/to/module
python -m pip install -e .
```